# 1. First, we define a function called greeting that takes a name parameter.
# 2. Then, we define a dictionary with two key-value pairs.
# 3. Finally, we call the greeting function and pass in the value associated with the Name key from the person dictionary.
def greeting(name):
    print("Hello, " + name)



person = {
    "Name" : "Zoro",
    "Name2" : "Sanji"
}